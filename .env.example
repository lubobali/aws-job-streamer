# Copy this file to `.env` and fill in the values. `.env` is gitignored — never commit it.
#
# Adzuna — free API key from https://developer.adzuna.com/admin/access_details
# Only needed for the Adzuna fetcher. Greenhouse, Lever and Ashby need no keys at all.
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
