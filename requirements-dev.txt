# Dev-only tooling. Prod deps live in requirements.txt.
# Installed with: uv pip install -r requirements-dev.txt
-r requirements.txt

ruff==0.14.6
pytest==8.4.2
pytest-randomly==4.0.1
pytest-xdist==3.8.0
respx==0.22.0
ty==0.0.1a23
